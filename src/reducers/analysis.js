// actions

const initialState = {

};

const analysis = (state = initialState, action) => {
	switch(action.type) {
		case 'something': 
			return state;
		default:
			return state;
	}
}

export default analysis;